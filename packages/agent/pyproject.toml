[project]
name = "nu-agent-core"
version = "0.0.0"
description = "Stateful agent loop and tool execution — Python port of @mariozechner/pi-agent-core"
requires-python = ">=3.13"
readme = "README.md"
license = { text = "MIT" }

dependencies = [
    "nu-ai",
    "pydantic>=2.10.0",
    "anyio>=4.7.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/nu_agent_core"]
